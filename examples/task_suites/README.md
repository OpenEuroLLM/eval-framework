These suites are examples that attempt to recreate the [OLMo3 pretraining evaluation configuration](https://arxiv.org/abs/2512.13961). They can be run with:

```bash
uv run eval_framework --llm-name <your_model> --task-suite examples/task_suites/olmo3_base_gen.py
```
